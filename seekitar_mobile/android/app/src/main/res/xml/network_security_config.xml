<?xml version="1.0" encoding="utf-8"?>
<!--
  Keamanan jaringan: HTTP polos (cleartext) DIBLOKIR secara default.
  Hanya host pengembangan yang diizinkan cleartext — produksi memakai
  HTTPS (https://seekitar.id). Tambahkan IP LAN dev di sini bila berubah.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulator Android: 10.0.2.2 = localhost host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <!-- Server dev berjalan di perangkat yang sama (tidak berubah) -->
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
